## What changed

## Why

## Verification

- [ ] `PYTHONPATH=src python -m unittest discover -s tests -v`
- [ ] `./kb.sh run --offline && ./kb.sh health`
- [ ] No personal notes, private feed URLs, credentials, or runtime ledgers are included
- [ ] User-facing behavior and changelog are updated when needed
